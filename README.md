# HKID Checksum Validator

A Java utility that validates Hong Kong Identity Card (HKID) numbers using the official checksum algorithm.

## How to Run
Compile:
javac src/com/example/hkid/*.java -d out

Run:
java -cp out com.example.hkid.Main
